'use strict';

define(['angular', 'angular-resource'], function (angular) {
    return angular.module('login', ['ngResource']);

});
// Declare app level module which depends on filters, and services
